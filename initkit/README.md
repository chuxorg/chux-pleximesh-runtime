# InitKit

The InitKit captures frozen constitutional artifacts that guide every slice in the mesh runtime.
Files placed here are treated as read-only references that drive planning and governance, not executable code.

Implementation logic, experiments, or mutable assets should never be stored in this directory.
